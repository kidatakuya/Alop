// import { Link } from "react-router-dom";

import './index.scss'

function MainText(props) {
    return(
        <>
            <p>
                {props.isText}
            </p>
        </>
    )
}

export default MainText;